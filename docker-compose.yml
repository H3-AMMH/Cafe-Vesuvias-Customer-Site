services:
  web:
    build: .
    volumes:
      - ./public:/app/public
      - /home/hmma/databases:/databases
      - /etc/ssl/cafe-menu:/etc/ssl/cafe-menu
    ports:
      - "3000:3000"
    env_file:
      - .env
